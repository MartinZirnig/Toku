import { GroupRemoveUserModel } from './group-remove-user-model';

describe('GroupRemoveUserModel', () => {
  it('should create an instance', () => {
    expect(new GroupRemoveUserModel()).toBeTruthy();
  });
});
