import { GroupAddUserModel } from './group-add-user-model';

describe('GroupAddUserModel', () => {
  it('should create an instance', () => {
    expect(new GroupAddUserModel()).toBeTruthy();
  });
});
