import { StoredMessageModel } from './stored-message-model';

describe('StoredMessageModel', () => {
  it('should create an instance', () => {
    expect(new StoredMessageModel()).toBeTruthy();
  });
});
