import { GroupUserAccessModel } from './group-user-access-model';

describe('GroupUserAccessModel', () => {
  it('should create an instance', () => {
    expect(new GroupUserAccessModel()).toBeTruthy();
  });
});
