import { GroupPictureModel } from './group-picture-model';

describe('GroupPictureModel', () => {
  it('should create an instance', () => {
    expect(new GroupPictureModel()).toBeTruthy();
  });
});
