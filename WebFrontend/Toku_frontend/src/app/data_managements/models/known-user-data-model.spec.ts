import { KnownUserDataModel } from './known-user-data-model';

describe('KnownUserDataModel', () => {
  it('should create an instance', () => {
    expect(new KnownUserDataModel()).toBeTruthy();
  });
});
