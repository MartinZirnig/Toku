import { ColorSettingsModel } from './color-settings-model';

describe('ColorSettingsModel', () => {
  it('should create an instance', () => {
    expect(new ColorSettingsModel()).toBeTruthy();
  });
});
