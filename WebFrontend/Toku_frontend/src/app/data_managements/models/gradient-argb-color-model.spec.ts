import { GradientArgbColorModel } from './gradient-argb-color-model';

describe('GradientArgbColorModel', () => {
  it('should create an instance', () => {
    expect(new GradientArgbColorModel()).toBeTruthy();
  });
});
