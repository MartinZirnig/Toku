import { GmailAuthorizationModel } from './gmail-authorization-model';

describe('GmailAuthorizationModel', () => {
  it('should create an instance', () => {
    expect(new GmailAuthorizationModel()).toBeTruthy();
  });
});
