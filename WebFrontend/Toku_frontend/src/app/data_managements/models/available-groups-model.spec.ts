import { AvailableGroupsModel } from './available-groups-model';

describe('AvailableGroupsModel', () => {
  it('should create an instance', () => {
    expect(new AvailableGroupsModel()).toBeTruthy();
  });
});
